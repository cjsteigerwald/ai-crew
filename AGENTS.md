# AGENTS.md

Repo-scoped instructions for coding agents working in `ai-crew`. This is the canonical
agent instruction file for this repository; `CLAUDE.md` points here.

## Agent skills

### Issue tracker

Issues live as GitHub issues in `cjsteigerwald/ai-crew`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
